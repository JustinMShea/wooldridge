# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages

r_packages:
  - wooldridge

after_success:
  - Rscript -e 'library(wooldridge)'
  
  
